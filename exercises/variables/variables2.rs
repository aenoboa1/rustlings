// variables2.rs
// Execute `rustlings hint variables2` or use the `hint` watch subcommand for a hint.


fn main() {
    // ADD Type annotations
    let x: i32 ;
    x = 10;

    // is tabnine working at all ?



    if x == 10 {
        println!("x is ten!");
    } else {
        println!("x is not ten!");
    }
}
